#!/bin/bash

if ! kiosk_mode=$(snapctl get kiosk-mode); then
    kiosk_mode=on
fi

if [ "${kiosk_mode}" == "off" ] || [ "${kiosk_mode}" == "on" ]; then
    echo "kiosk-mode=$kiosk_mode" > $SNAP_DATA/.config
else
    echo "kiosk-mode can only be set to [off|on]"
    exit 2
fi

